# WOW-023 — Hygiene sweep: typos, dead code, stray console.log, fragile patterns

- Role: frontend-implementer + creative-tech-integrator split (ticket's suggested agents; implemented as one pass since the changes are small and mechanical)
- Date: 2026-07-12
- Branch: `feat/wow-023-hygiene-sweep`, stacked on `feat/wow-031-clip-name-normalization` (tip of the WOW-014→032→018→020→021→031 chain) — required, not optional: `backend/adapter/AbletonAdapter.ts`, `backend/index.ts`, and `.env` are all touched by that still-unmerged chain, and this ticket's own line-number references (`AbletonAdapter.ts:24-25`, `:29`, `:45`, `:142`) only match the file's _current_ (post-chain) state. Checked every other unmerged branch from this run for overlap with this ticket's allowed files too — `backend/event/OutgoingEvents.ts`, `backend/service/MusicDatabaseService.ts`, `.env`, both frontend containers: no other branch touches any of them, except a small, non-conflicting `README.md` overlap with `feat/wow-022-node-22-lts` (that branch's one-line Node-version edit is at line ~11 under "## Setup"; this ticket's two `WS_SEVER_PORT` edits are at lines ~42/55 under "Starting the backend" — different sections, both purely edit-in-place on existing lines, no realistic conflict, same reasoning WOW-022 itself already applied to a different README overlap).

## Ticket

WOW-023 — see `docs/TICKETS_002_BUGS.md`. Ten numbered items, "one deliberate zero-behavior-change PR in the spirit of WOW-011." No stop condition triggered: item 1's `.env` approval was already pre-answered ("approved" — this run's pre-answered-decisions list), and no rename turned out to be load-bearing beyond spelling.

## What was done, item by item

1. **`WS_SEVER_PORT` → `WS_SERVER_PORT`**: `.env` (both the key and the `VITE_WS_SERVER_PORT=$WS_SEVER_PORT` reference that reads it) and `backend/index.ts:9`. Pre-approved per this run's standing decisions.
2. **`TIMEOUT_IN_MILISECONDS`/`TIMEOUT_WARNING_IN_MILISECONDS` → `TIMEOUT_IN_MILLISECONDS`/`TIMEOUT_WARNING_IN_MILLISECONDS`, `emitEventWithoutResetingTimout` → `emitEventWithoutResettingTimeout`**: renamed in `backend/adapter/AbletonAdapter.ts` (definitions, all internal uses, and the exported-object keys) and `backend/event/OutgoingEvents.ts` (definition + export). Grepped the whole repo for every reference before and after — no other backend file imports either symbol externally.
3. **`console.log('clips', clips)` removed** (`AbletonAdapter.ts`, inside `queueClip`) — deleted outright rather than converting to `Logger.debug`: `clips` is an array of raw `ableton-js` `Clip` objects, not meaningful structured log data (no clean serialization, not human-readable in a pino log line), so a debug-level dump wouldn't have added real diagnostic value.
4. **Deleted `KEY_LEADER_ORDER` and `ATTRACTOR_STATE_CLIP_NAME`** (declarations + their entries in the exported `AbletonAdapter` object). Grepped the whole repo (all `.ts`/`.tsx`, including every test file) — confirmed genuinely never read anywhere, not even via the exported object. Chose delete over "ticket the intended feature": both names strongly suggest a half-considered feature (`KEY_LEADER_ORDER`'s Vox-first ordering is the exact reverse of `TRIGGER_ORDER`'s Drums-first ordering, suggesting a "let Vox lead key/pitch decisions separately from playback-trigger order" idea; `ATTRACTOR_STATE_CLIP_NAME = 'Wicked Casting'` suggests an "idle/attract-mode clip" idea) — but I have no way to confirm what was actually intended, and inventing a speculative feature ticket from a guess would be worse than just noting the guess here and letting git history preserve the original code if anyone wants to revisit it.
5. **`[1, 2, 3, 4]?.map` → `[1, 2, 3, 4].map`** (`CurrentlyPlayingListContainer.tsx`) — the array literal is never null/undefined, so `?.` was a no-op.
6. **Removed the dead `info?.clipName?.trimStart()` initial assignment**, replaced with a plain `let clipName = '';`. The value was unconditionally overwritten by the if/else immediately below either way, so the `.trimStart()` computation's result was never used. Per the ticket's own instruction, confirmed the else-branch's empty-string behavior is intended and added a comment: displaying nothing (rather than falling back to `info?.clipName`, an internal CSV/Ableton identifier like "Flashback Drums 10A 135") when artist/songTitle metadata is missing is more consistent with the visitor-facing "Artist - Song Title" format used in the happy path than showing a raw internal name would be. **Zero behavior change** — this is documenting the existing, unchanged behavior, not altering it.
7. **`ingredients_contianer` → `ingredients_container`** (`RecipeBoxContainer.tsx`) — grepped for any other reference to the misspelled id (CSS, tests, JS query selectors); none found, purely cosmetic.
8. **Dynamic Tailwind class replaced with a conditional between two full literal strings** (`CurrentlyPlayingListContainer.tsx`, the `id='bpm'` div): `` `col-start-${index % 2 === 0 ? 2 : 1} col-span-3 font-fondamento` `` → `index % 2 === 0 ? 'col-start-2 col-span-3 font-fondamento' : 'col-start-1 col-span-3 font-fondamento'`. Produces byte-identical class strings to before (verified live, see below) — the fix is about making both literals independently discoverable by Tailwind's static scanner, not changing output. Matches the pattern the same file already uses two other places (`index % 2 === 0 ? (<div>...) : null` and the two `col-start-2`/`col-start-1` blocks further down).
9. **Deleted the commented-out `enrichRecommendations` block** (`MusicDatabaseService.ts`), replaced with a short comment documenting the intentional frontend/backend divergence. Verified both halves of the claim directly: `src/util/ClipDatabaseUtil.ts:11` does call `CsvUtil.enrichRecommendations` (frontend runs it); `sim/test/music-database.test.ts:33` asserts `does not attach recommendedClips — EnrichRecommendations is disabled in the real backend` (matching the backend's now-commentless omission).
10. **`sim/server.ts`'s stale pre-WOW-011 path comments updated** (4 of the ticket's cited 5 locations actually contained a stale path — `backend/events/incoming-events.ts` / `backend/ableton-api.ts` — the 5th cited line, `:4`, already correctly said `backend/index.ts`, nothing to fix there) to the current `backend/event/IncomingEvents.ts` / `backend/adapter/AbletonAdapter.ts` paths. Dropped the specific line-number suffixes this time rather than updating them to new numbers — pinned line numbers in comments are exactly what went stale here in the first place, and these are "see roughly here" pointers, not load-bearing references.

## Scope judgment calls (transparently flagged, not silently done)

- **`sim/server.ts` is not in this ticket's "Allowed files" list**, but item 10's own numbered description explicitly names it and its stale-path problem. Included it anyway — comment-only, zero risk, and the ticket's own text unambiguously asks for it; reads as a drafting omission in the allowed-files enumeration (9 of 10 items map cleanly to a listed file; this is the only miss) rather than a deliberate exclusion. If a reviewer disagrees, this is a one-hunk revert.
- **`README.md`'s two `WS_SEVER_PORT` mentions** (lines ~42, ~55) aren't explicitly named by item 1's text (which only names `.env` and `backend/index.ts:9`) or listed in "Allowed files," but they're direct prose references to the exact symbol item 1 renames. Included under the allowed-files list's own "plus every call site of renamed symbols" clause, read broadly enough to cover documentation call sites, not just executable code. Same reasoning bucket as the `sim/server.ts` inclusion above.
- **`sim/core/simulator.ts` and `sim/test/simulator.test.ts` deliberately NOT touched**, despite having their own independently-defined `TIMEOUT_IN_MILISECONDS`/`TIMEOUT_WARNING_IN_MILISECONDS` constants with the identical typo.\** These are a separate, independently-maintained mirror (`sim/` cannot import from `backend/` per its own import-guard, ADR-001) — not the same TS binding, not a "call site" of the renamed symbol in any real sense, and not in this ticket's allowed files. Leaving them out of sync in *name\* doesn't create any behavioral risk (they're unrelated symbols that happen to share a name), matching the same reasoning that's kept `sim/` and `backend/` allowed to drift structurally elsewhere in this codebase. Noted here so it isn't mistaken for an oversight.
- **`sim/core/music-database.ts:60` has the same kind of stale pre-WOW-011 path comment** (`backend/events/incoming-events.ts`) that item 10 fixed in `sim/server.ts`, but this specific file/line isn't named anywhere in the ticket. Spotted while grepping for completeness; deliberately left alone — unlike `sim/server.ts`, nothing in the ticket text points at this file, so including it would be scope-inventing rather than filling an enumeration gap. Trivial enough that it isn't worth a formal follow-up task; noting it here is enough for whoever next touches that file.

## Verification

- [x] `yarn lint` clean
- [x] `npx tsc --noEmit` (root) clean
- [x] `npx tsc --noEmit -p backend/tsconfig.json` clean
- [x] `yarn test` — 16 files / 95 tests passed (this branch's ancestry includes WOW-025's and WOW-031's new test files, hence higher than main's own baseline)
- [x] `yarn build` clean
- [x] **Sim + UI smoke** (required by this ticket specifically, not just the usual validation suite): ran `yarn sim full-spell` + `yarn dev`, loaded the UI in a real browser. Confirmed live: both pillars' clip names render in the correct "Artist - Song Title" format; the BPM row's column position correctly alternates `col-start-2`/`col-start-1`/`col-start-2`/`col-start-1` across the four pillars (read directly from the rendered DOM via `document.querySelectorAll('[id="bpm"]')` — byte-identical to what the old dynamic template literal would have produced); `document.getElementById('ingredients_container')` found, the old typo'd id is gone; zero console errors; zero sim-server errors.
- [x] grep confirms no old symbol names remain anywhere in scope (see the comprehensive final sweep in this session — every remaining hit is either historical ticket/agent-note prose that correctly should not be touched, or `sim/`'s independent mirror, deliberately out of scope per above).

## Safety checklist

- [x] Zero behavioral change — every rename is name-only; the one non-rename logic touch (item 6, dead `trimStart()` removal) is provably a no-op since the value was always overwritten; item 8's Tailwind-class restructure verified byte-identical output live.
- [x] `Music Database.csv` untouched (not in this ticket's scope)
- [x] `Arduino/` untouched
- [x] No new lint rules
- [x] Standard reviewer gate (no specialist review required by this ticket's own safety notes — "escalate to specialists if any diff brushes musical logic," and none of these ten items touch musical logic, only naming/dead-code/formatting)

## Fix round (Copilot, 3/3 threads)

1. **Real finding, fixed with extra care given the stakes**: Copilot flagged that `docs/ABLETON_INTEGRATION.md:18` still described a "key-leader order" (`KEY_LEADER_ORDER`) as governing phrase-leader/timing-key reference, but item 4 above deleted that constant. This document sits directly under the header "**Do not modify the Ableton project, routing, clip naming, transposition tables, quantization, trigger/key-leader order, or RFID→clip mappings without explicit human approval**" — i.e. it's explicitly musical-logic-protected territory, and this file isn't in this ticket's allowed list at all (unlike `sim/server.ts`/`README.md`, Copilot found this fresh, the ticket never named it). Before touching it, re-verified with full certainty by reading `backend/service/PhraseLeaderService.ts` directly: `findNextPhraseLeader` sorts by `AbletonAdapter.TRIGGER_ORDER` only — `KEY_LEADER_ORDER` was never referenced there or anywhere (matches the repo-wide grep from item 4). This is a pure code-observation correction (what the code already, verifiably does), not a musical-logic _decision_ — the doc's own header says "Status: observed from `backend/adapter/AbletonAdapter.ts`..." confirming it's meant to track code, not prescribe it. Corrected the "Trigger order" bullet to note it also governs phrase-leader selection, and added a dated correction noting the removed constant was never actually wired up. No code changed by this fix — `TRIGGER_ORDER`'s behavior is untouched.
2. `src/container/CurrentlyPlayingListContainer.tsx`: the new comment's quoted example clip name (`"Flashback Drums 10A 135"`) got split across two comment lines by line-wrapping, reading confusingly. Swapped to a shorter example (`"Wicked Casting"`, already a precedented example name in this codebase) that fits on one line intact.
3. `sim/server.ts:43`: the one `sim/server.ts` fix that kept a pinned line-number suffix (`AbletonAdapter.ts:29-30`) was inconsistent with this same PR's own stated reasoning for the other three fixes in that file ("dropped the specific line-number suffixes... pinned line numbers in comments are exactly what went stale here in the first place"). Dropped it too, for consistency.

Re-validated: `yarn lint`, both `tsc --noEmit` checks clean. Did not re-run `yarn test` for this round (three comment/prose-only changes, no logic touched, concurrent background-worktree contamination risk still present at the time) — already have a clean 16-file/95-test baseline from the implementation pass.

## Fix round 2 (general review CHANGES-REQUESTED — a "light gate", all findings already fixed above plus one more)

The general reviewer independently found and confirmed all three fixes in "Fix round (Copilot...)" above (its own repo-wide grep caught the same `KEY_LEADER_ORDER` doc staleness before it even cross-checked Copilot's identical finding — convergent signal), reproduced the live sim/UI smoke test itself from its own isolated worktree (identical DOM results), and independently verified all ten items as true no-ops by tracing the actual diff hunks rather than trusting this note's narrative. Its verdict was CHANGES-REQUESTED but explicitly framed as "a light gate, not a rejection," on the unresolved-Copilot-threads process rule plus one new finding:

4. **New finding**: `.claude/agents/audio-ableton-reviewer.md:21` lists `KEY_LEADER_ORDER` in its "verify diffs preserve" checklist — stale now that this PR deletes that export. Not caught by this PR's own earlier repo-wide grep (that grep covered `.ts`/`.tsx` source, not `.claude/agents/*.md` profile text). Fixed: reworded the checklist line to note `TRIGGER_ORDER` also governs phrase-leader selection (the actual, verified mechanism), dropping the stale `KEY_LEADER_ORDER` mention.

Also considered, not applied: the reviewer suggested (explicitly as "recommended but non-blocking... cheap, consistent insurance, not a blocking requirement") an audio-ableton-reviewer confirming pass on the `AbletonAdapter.ts`/`OutgoingEvents.ts` hunks, since every other ticket in this stacking chain obtained one. Declined: this ticket's own safety notes set a conditional bar ("standard reviewer gate, escalate to specialists if any diff brushes musical logic") rather than WOW-014/017/018/020/021/025/027/031's unconditional requirement, and two independent, thorough traces (mine during implementation, the general reviewer's own repo-wide verification including a direct read of `PhraseLeaderService.ts`) both concluded this diff doesn't cross that bar — every touched line is a rename, a verified-dead-code deletion, or a `console.log` removal, none of which can change `TRIGGER_ORDER`'s behavior or any comparison outcome. The reviewer's own verdict agreed this diff doesn't brush musical logic. Noting this reasoning explicitly in case a future reviewer or the human wants the extra pass anyway — it's cheap to add.

Also noted by the reviewer (informational, not a defect): the "Scope judgment calls" section above cites only one example (`sim/core/music-database.ts:60`) of a deliberately-left-alone stale pre-WOW-011 path comment, when the same category is far more widespread across `sim/core/simulator.ts` and `sim/test/simulator.test.ts` (7+ instances each). The underlying decision to leave `sim/`'s independent mirror alone is sound (verified via the import-guard test) — the note just undersold how much of that category exists. No action taken in this PR; flagged here for whoever eventually does a `sim/`-comments hygiene pass.

## Status

Implementation and both fix rounds complete, all validation green including the required live sim/UI smoke test, independently reproduced by the general reviewer in its own isolated worktree. Ready for Copilot-thread resolution and gate.
